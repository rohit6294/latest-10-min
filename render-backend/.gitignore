node_modules/
.env
*.log
serviceAccount.json

# Override the root .gitignore that excludes **/lib/**/*.js (meant for the
# Cloud Functions compiled output, not our hand-written lib/ source).
!lib/
!lib/**
